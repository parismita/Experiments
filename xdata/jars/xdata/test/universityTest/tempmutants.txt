7|select count(id) from student group by dept_name
7|select dept_name, count(DISTINCT id) from student group by dept_name
7|select dept_name from student group by dept_name
7|select dept_name, count(id) from student group by dept_name, name
7|select dept_name, count(id) from student group by dept_name, id
7|select dept_name, count(id) from student group by name
7|select dept_name, count(id) from student group by id
7|select dept_name, count(id) from student group by tot_cred
7|select dept_name, count(name) from student group by dept_name
